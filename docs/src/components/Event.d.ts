/// <reference types="react" />
import { RecentEvent } from '../state';
export declare function Event({ event }: EventProps): JSX.Element;
export declare type EventProps = {
    event: RecentEvent;
};
